services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      DATABASE_URL: file:/app/data/dev.db
    volumes:
      - db_data:/app/data
      - uploads:/app/public/uploads

volumes:
  db_data:
  uploads:
